{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": { "outputs": ["dist/**"] },
    "lint": { "dependsOn": ["^build"] },
    "test": { "dependsOn": ["build", "^build"] }
  }
}
